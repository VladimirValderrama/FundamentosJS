// Existen datos primitivos y complejos

// Primitivos:
// string, number, boolean, null, undefined, symbol y bigint

// Complejos: 
// object, array, function

// PRIMITIVOS
    // string
    let nombre = 'Tere';

    // number
    let edad = 25

    // boolean
    let esMayorDeEdad = true;

    // null: un VACÍO, no existe nada de nada.
    let noHay = null;

    // undefined
    let noDefinido = undefined;

    // symbol: para asignar valores unicos
    let simboloUnico = symbol('unico');

    // bigint: grandes valores numericos
    let numeroGrande = 2n

// COMPLEJOS
    // object: varias caracteristicas de algo
    let carro = {
        marca: 'Tesla',
        modelo: 'Model S'
    }

    // array o listas
    let frutas = ['manzana', 'banana', 'uvas']

    // function en las parentesis va la info que quiero que llegue a la funcion y sera necesaria, luego en las llaves va el CUERPO de la funcion
    functionHola(pepito)

    